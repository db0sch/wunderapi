require "spec_helper"

describe Wunderapi do
  it "has a version number" do
    expect(Wunderapi::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
